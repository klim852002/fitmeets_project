<%= render 'partials/herointro' %>

<br/><Br/>

<%= react_component 'Event', {event: @event} %>
<!-- equivalent to -->
<!-- link_to "Join", { controller: "events", action: :join }, method: :post -->
<hr>
<div class= "hold-eventform-container">
  <button class = "draw"><%= link_to 'Back', events_path %></button>
  <button class = "draw"><%= link_to "Join Event", join_event_path, method: :post %></button>

  <h3>These players have joined!</h3>
  <% @eventusers.each do |eventuser| %>
    <ol>
        <li>
            <a href="#"><%= eventuser.name %></a>
            <!-- <%= eventuser.email %><br>
            <%= eventuser.contact_number %><br> -->
            <hr>
        </li>
    </ol>
  <%end%>
</div>


<div style='clear:both;'></div>

<hr/>

<%= render 'comments/form' %>

<%= render 'comments/comment' %>
